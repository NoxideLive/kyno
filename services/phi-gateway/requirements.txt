# Runtime deps — installed in the phi-gateway Docker image.
# Boot: docker compose up --build phi-gateway (see docs/domain-gateway-runbook.md)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
